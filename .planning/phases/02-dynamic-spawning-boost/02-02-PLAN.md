---
wave: 2
depends_on: [02-01]
requirements_addressed: [MECH-02, MECH-05]
files_modified: [Assets/Scripts/SpawnerSystem.cs]
autonomous: true
---

# Plan 02-02: Multi-Row Spawning & Guarantee

Implement the decay-based spawn logic, catch-up guarantee, and stacked-ring Boost patterns.

## Objective
Provide the specialized spawning behaviors required for the Boost state and the various stage-based difficulty triggers.

## Tasks

<task id="1" type="execute">
<title>Decline-based Random Boost Spawn</title>
<read_first>
- Assets/Scripts/SpawnerSystem.cs
</read_first>
<action>
1. Add `GetBoostChanceForStage()` helper:
    - Stage 1: 0.01 (1%)
    - Stage 2: 0.008 (0.8%)
    - Stage 3: 0.006 (0.6%)
2. Update `SpawnAt()` or `TrySpawn()`. Before spawning a normal lantern, check if a Boost lantern should spawn based on the chance and if `GameManager.Instance._hasBoostTriggeredThisStage` is false.
</action>
<acceptance_criteria>
- Spawner checks stage-specific boost chance.
- Boost trigger only spawns if none triggered yet this stage.
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Implement 20s Catch-up Guarantee</title>
<read_first>
- Assets/Scripts/SpawnerSystem.cs
- Assets/Scripts/GameManager.cs
</read_first>
<action>
1. In `SpawnerSystem.Update()`, check if `GameManager.Instance.TimeRemaining` is close to 10.0f (meaning 20s have passed since timer starts at 30s).
2. If time is 10.0s, score is below threshold, and no boost triggered, force spawn a Boost lantern at a random free spawn point.
</action>
<acceptance_criteria>
- Boost lantern guaranteed at the 20s mark if criteria met.
</acceptance_criteria>
</task>

<task id="3" type="execute">
<title>Implement Stacked Ring Boost Pattern</title>
<read_first>
- Assets/Scripts/SpawnerSystem.cs
</read_first>
<action>
1. Implement `StartBoostSpawning()` which:
    - Clears current spawning timers/waves.
    - Determines number of rows based on `CurrentStage` (2, 3, or 4).
    - Spawns matching rings of lanterns at vertical offsets (e.g., 0.5m apart).
    - Ensures all spawned lanterns during this state are `LanternType.Boost`.
</action>
<acceptance_criteria>
- Boost state spawns 2 rows in Stage 1, 3 in Stage 2, and 4 in Stage 3.
- All lanterns in Boost mode are Yellow/Boost type.
</acceptance_criteria>
</task>

## Verification Criteria

### Manual Verification
1. Wait for 20s mark without reaching threshold. Verify Boost lantern appears.
2. In Stage 1 Boost, verify 2 rows of lanterns spawn.
3. In Stage 3 Boost, verify 4 rows of lanterns spawn (check for performance/pooling limits).

## must_haves
- Boost chance decays per stage (1%, 0.8%, 0.6%).
- 20s guarantee works reliably.
- Correct row counts (2/3/4) for each stage's boost.
